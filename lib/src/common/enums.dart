enum PlayerType {
  offence,
  defence,
  disc, //used outside of component to determine a disc or player is needed
}

enum MovementDirection {
  north,
  east,
  south,
  west,
  norteast,
  northwest,
  southeast,
  southwest,
}

enum BaseFormation {
  verticalStack,
  horizontalStack,
}